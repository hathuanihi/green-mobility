name: core_network
description: Shared Network, Dio HTTP Client & STOMP WebSocket Client for Green Mobility
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.4.3+1
  stomp_dart_client: ^1.0.0
  flutter_secure_storage: ^9.2.2
  core_model:
    path: ../core_model

dev_dependencies:
  lints: ^3.0.0
