name: core_map
description: Shared Map, Marker Interpolation & Routing for Green Mobility Flutter
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.6.1
  flutter_polyline_points: ^2.1.0
  geolocator: ^12.0.0
  core_model:
    path: ../core_model

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
